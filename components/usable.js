(function (Crafty, document, window) {
	Crafty.c("Usable", function() {
		_effect: '',
		_target: '',	// ally or enemy
		_range: '',		// one or all
		
		init: function() {
		},
		
		use: function() {
			// crap, I actually have no idea what goes here yet
			// probably going to end up returning a list of parameters
			// which means this function will need to be named differently.
		},
	});
});